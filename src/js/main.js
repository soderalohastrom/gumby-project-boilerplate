$.handlebars({
    templatePath: 'partials',
    templateExtension: 'hbs'
});

// Load partials in columns
/*$('.nav-col').render('nav', {

});*/


$(document).ready(function() {
  console.log("Ready");
});
